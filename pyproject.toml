[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nabeatsu-jtalk"
version = "0.1.0"
description = "3の倍数でアホになる改造版Open JTalk"
authors = [{name = "maki249"}]
readme = "README.md"